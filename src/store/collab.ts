import { CollabState } from "@/types/state";

const state: CollabState = {
  null: null,
};

const getters = {};

const actions = {};

const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
